const initialState = {
  fetching: false,
  fetched: false,
  smurfs: [],
  error: ''
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
